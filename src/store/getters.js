const getters = {
    siteName: state => state.siteName,
    lessons: state => state.lessons,
    storeCurrency: state => state.storeCurrency,
    cart: state => state.cart,
}

export default getters;